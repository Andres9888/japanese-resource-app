scalar IsoDate #declered for all .graphql files

type Listing {
    id: ID!
    title: String!
    address: String!
    count: Int!
  }

type Query {
  clients: [Client]
  client(id: ID!): Client
}

  type Query {
    listings: [Listing!]!
  }

 type Mutation {
    increment(): {CountResult}
  }

  type CountResult {
    
  acknowledged: Boolean,
  }
`

type Client {
  id: ID!
  username: String
  email: String
  name: String
  bio: String
  avatar: String
  createdAt: IsoDate
}




  
  
