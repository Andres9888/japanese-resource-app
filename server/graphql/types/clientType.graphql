scalar IsoDate #declered for all .graphql files

type Listing {
    image: String!
    url: String!
    title: String!
    description: String!
    count: Int!
  }

  type Query {
    listings: [Listing!]!
  }
  type CountResult {
  acknowledged: Boolean
  }
 type Mutation {
    increment: CountResult
  }




  
  
